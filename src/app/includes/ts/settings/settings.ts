export const CLIENT_SETTINGS = {
	SERVER_URL: {
		PROTOCOL: "http",
		HOST: "localhost",
		PORT: 80,
	},
} as const;

export const SERVER_SETTINGS = {
	LISTEN: {
		HOST: "localhost",
		PORT: 80,
	},
};
