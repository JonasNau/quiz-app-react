export enum ESocketEventNames {
	NEW_CONNECTIION = "connection",
	INIT_QUIZ = "init_quiz",
	JOIN_ROOM = "join_room",
	LEAVE_ROOM = "leave_room",
	ERROR = "error",
	SUCCESS = "success",
}

export enum ERoomNames {
	REFERENT = "referent",
	BEAMER = "beamer",
}
